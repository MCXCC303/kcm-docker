# ---------------------------------------------------------------------------
# 构建输出（CMake / ECM）
#
# 本项目按 README 的做法在源码树内使用 build/ 作为构建目录，因此必须忽略，
# 否则每次构建产物（54MB 左右）都会被 git 看到。
# ---------------------------------------------------------------------------
build/
build-*/
cmake-build-*/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
DartConfiguration.tcl
ecm_uninstall.cmake
install_manifest.txt
Testing/
compile_commands.json
prefix.sh

# 编译中间产物（正常只出现在 build/ 内，这里兜底防止误提交）
*.o
*.a
*.so
*.so.*
moc_*.cpp
moc_*.h
qrc_*.cpp
ui_*.h
*_autogen/
*.moc

# 翻译产物（.po / .pot 是源文件，必须提交；.mo 由构建生成）
*.mo

# ---------------------------------------------------------------------------
# 编辑器 / IDE
# ---------------------------------------------------------------------------
*.user
*.user.*
*.kdev4
.kdev4/
.kateproject
.kateproject.*
*.swp
*.swo
*~
*.autosave
.vscode/
.idea/
.cache/

# ---------------------------------------------------------------------------
# 本地临时文件（不属于项目内容）
#
# probe* 是调试期留下的独立探针程序，不参与 CMake 构建。
# 确认不再需要后可以直接删除，届时本段也可一并移除。
# ---------------------------------------------------------------------------
/probe*
*.svg
