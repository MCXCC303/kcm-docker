# 变更记录

本文件记录 Kontainer 的对外可见变更（ARCH_V3_pre §2.14）。

格式约定：

- 版本号遵循 `MAJOR.MINOR.PATCH`。当前尚未发布正式版本，各期为开发里程碑。
- **权限模型变化**（例如从「依赖用户在 socket 所属组」改为「通过 polkit 提权」）、
  **新增破坏性操作**（remove / prune / 删除数据卷）、**提权范围扩大**这三类变更，
  必须在对应条目标注 `⚠️ 权限` 并单独成段说明影响，同时提升次版本号，
  以便用户与分发渠道（AUR / 各发行版打包）能清楚感知变化。

---

## 六期（0.6.0）

看得见运行中的容器，管得住网络与数据（[ARCH_V5_V8.md](ARCH_V5_V8.md)）。

> ⚠️ **权限**：六期没有新增任何提权能力（仍然只按 Docker socket 的实际权限工作，
> 不引入新的 polkit 动作、不调用 `docker` CLI），但**破坏性操作明显变多**，因此单列说明：
>
> - 新增写操作：创建 / 删除**网络**、把容器**连接 / 断开**网络、创建 / 删除**数据卷**、
>   **清理未使用的数据卷**（prune）。
> - 删除网络与删除/清理数据卷是**不可撤销**的：确认对话框里固定写明后果
>   （"已连接的容器会失去该网络"、"卷里的数据会一起消失"、"只会删除没有任何容器使用的数据卷"）。
> - 删除数据卷**不提供 force**：被容器使用时由引擎拒绝，界面说明原因，不替用户强删。
> - 清理（prune）**先列出将被删除的卷名与可回收空间**再确认；使用情况未知的卷**不在**清理范围内
>   （"未知"不等于"未使用"）。
> - 只读模式下这些入口**整体不出现**（不是禁用后静默）；socket 不可写时整页仍是只读。

新增能力：

- **容器日志**（ARCH_V5_V8 §3.1）：容器详情「日志」分区实时跟随输出——等宽控制台、跟随/暂停
  （暂停期间继续接收并在恢复时一次补上）、清空、复制全部、断开与重新连接；按 `Config.Tty`
  分形态解复用（TTY 原始字节 / 非 TTY 的 8 字节帧），剥掉 ANSI 颜色、`\r` 进度条按"覆盖当前行"处理；
  行数与字节**双上限**（5000 行 / 512 KiB），超出时提示省略了多少行；容器停止是正常结束（给"重新连接"），
  日志驱动不支持读取时给出替代做法。**只在日志分区打开时读取**
- **网络管理**（§3.2–§3.4）：新增「网络」标签页（搜索 / 内置与自建过滤 / 排序）与网络详情
  （子网、网关、成员容器**可点击跳转**、标签与驱动选项）；创建 bridge 网络（名称/子网/网关/
  内部/可附加/标签，实时校验）；删除网络（内置网络不出现删除入口）；容器详情可把容器
  **连接 / 断开**网络（支持别名），断开写明"可能中断通信"
- **数据卷管理**（§3.5–§3.6）：新增「数据卷」标签页（搜索 / 未使用与使用中过滤 / 排序，
  卡片区分"使用中 / 未使用 / 使用情况未知"）与卷详情（挂载点可复制、可在文件管理器中打开、
  标签与选项）；创建卷；删除卷（不提供 force）；清理未使用卷（**先预览**再确认）；
  存储条图例里的「数据卷」可直接跳到卷列表
- **修复**：仓库凭据校验此前**总是失败**——`/auth` 的凭据必须放在**请求体**里，只发
  `X-Registry-Auth` 头时引擎会回 400 `invalid X-Registry-Auth header`（实测四种头编码都一样）。
  现在按 docker CLI 的形态提交；另外把引擎在连不上仓库时回的 500（`context deadline exceeded`）
  正确归类为"仓库不可达"，而不是"凭据被拒"
- 端口拓扑：同一个容器端口的多个宿主绑定合并成**分支连线**（左列只出现一枚芯片），
  绑定行留出更多垂直空间，起点圆环与最下方那条分支同色

**权限**：见上方 ⚠️ 说明。次版本号从 0.5.0 提升到 0.6.0（新增网络与数据卷的写操作）。

---

## 五期（0.5.0）

连接与信任：daemon 运行时配置变更（**首次引入提权**）、仓库认证与凭据管理（[ARCH_V5_V8.md](ARCH_V5_V8.md)）。

> ⚠️ **权限**：五期第一次让 Kontainer 具备提权能力，范围被严格限制并且**可降级**。
>
> - 新增一个**受限 helper**（`kontainer_helper`）与两个 polkit 动作：
>   `org.kde.kontainer.daemon.save`（把白名单键合并进 `/etc/docker/daemon.json`）、
>   `org.kde.kontainer.daemon.restart`（经 systemd 的 D-Bus 重启 `docker.service`）。
>   helper 只接受白名单键的**编辑意图**：路径、单元名都是编译期常量，不接受任意 JSON/路径/命令，
>   写前自动备份、原子写入，看不懂的现有文件拒绝覆写。
> - polkit 默认 `auth_admin_keep`（活动会话，约 5 分钟）：界面上的「解锁以编辑」就是这段时间的
>   显式入口，到期自动重新上锁。用户可取消，取消后功能退回**可复制的手动命令**，不会静默失败。
> - 系统级配置页需要管理员权限；用户级配置（rootless）本来就可以直接编辑，不需要解锁。
> - **凭据**：仓库登录凭据只存 **KWallet**（专用 `Kontainer` 文件夹），**不写明文文件、
>   不回退到明文**；`~/.docker/config.json` 只做**一次性只读导入**（不修改该文件、
>   不覆盖钱包里已有的条目、不调用 credential helper）。拉取时凭据只出现在
>   `X-Registry-Auth` 请求头里，不进 URL、不进日志。
> - 本期的提权只影响"改 `/etc/docker/daemon.json` 与重启 docker 服务"这两件事：
>   不修改 socket 权限、不改动其他系统文件、不调用 `docker` CLI。

新增能力：

- **系统运行时配置**（ARCH_V5_V8 §2.2–§2.5）：部署形态探测（系统级 / rootless / 未知，
>   实测本机是 `DockerRootDir` 在家目录的系统级守护进程）、配置文件定位与可写性判定、
>   只读展示"其他键原样保留"、镜像加速器与 insecure-registries 列表编辑、
>   并发下载数与日志驱动编辑（「默认」= 删除该键）、保存前自动备份、
>   重启 Docker（含"将停止 N 个容器"的后果说明）、恢复上一版本、写后即读
- **仓库认证**（ARCH_V5_V8 §2.6–§2.7）：KWallet 凭据存储、`POST /auth` 校验后保存、
>   令牌登录、测试连接、移除（二次确认）、从 Docker CLI 一次性只读导入（逐条勾选）、
>   私有仓库拉取自动带上凭据；镜像标签页工具栏与镜像详情提供「仓库登录…」，
>   401/403 的拉取失败与"该仓库还没登录"的拉取对话框都会直接引导到登录框
- **界面**：用户级 / 系统级配置分成两个入口（作用域与"哪个 daemon 读它"分开说明）；
>   自动刷新不再抹掉正在编辑的内容；端口拓扑连线改为按容器确定性取色的彩色线；
>   挂载行的容器路径靠右对齐、宿主路径过长时中间省略

**权限**：见上方 ⚠️ 说明。次版本号从 0.4.0 提升到 0.5.0（新增提权组件与凭据存储）。

---

## 四期（0.4.0）

首批写操作 · 挂载与端口信息架构（[ARCH_V4.md](ARCH_V4.md)）。

> ⚠️ **权限**：四期起 Kontainer 不再是只读工具。写操作**按 socket 实际权限**工作，
> 不引入任何提权机制（无 KAuth、无 polkit policy、不修改 socket 权限、不调用 `docker` CLI）。
> 启动时探测 socket 可写性：不可写时**写入口整体不出现**，并说明原因；
> 运行中若引擎返回 403 / EACCES，本次会话降级为只读。
> **注意：使用系统级（root daemon）Docker 时，把用户加入 docker 组等价于给予 root 权限。**
> 本项目的目标部署形态仍是 rootless Docker（socket 由用户自己拥有），此时不涉及该风险。

新增能力：

- 容器操作：启动 / 停止 / 重启 / 删除（删除不删除数据卷，且强制二次确认，并说明后果）
- 镜像操作：**拉取列表**——可以同时拉取多个不同镜像，关掉拉取对话框不会中断，
  进度与结果常驻镜像标签页；每一路都能单独取消；**失败记录带着引擎原文保留**，
  直到用户处理掉（拉取失败不再被静默）；删除（单标签只删该标签；
  多标签需显式选择「删除全部标签」，走 `force`）。拉取为匿名拉取，不读取任何 registry 凭据
- 修复：拉取对话框里「已经在拉取」的提示调用了一个未暴露给 QML 的方法
  （`rowForReference` 缺 `Q_INVOKABLE`），输入镜像名时每敲一个字符都会抛一次
  `TypeError`；同类问题（`Button.trigger()`）也一并修掉，并把「QML 报错捕获」
  改成每个测试用例都生效（之前只在第一个用例生效，所以这类错误连续两次逃过测试）
- 拉取失败会快速给出可操作说明：引擎在联系镜像仓库时若长时间**一个字节都不回**
  （网络 / 代理 / IPv6 无出口），10 秒内没有响应头就判定「拉取没能开始」，
  而不是干等一分钟；失败分类与引擎原文也会写进日志
- 写操作地基：全部 REST 路径集中到 `backend/docker_api_paths.h`；传输层支持 POST / DELETE、
  流式响应增量解析与取消；`DockerError` 增加 409（状态冲突）分类，错误分为
  「用户可自行解决 / 环境问题 / 意外」三类，UI 呈现不同
- 挂载分区重构：类型 / 读写模式 / 命名卷名 / 宿主路径存在性，并可在**系统文件管理器中打开宿主目录**
  （唯一的非 Docker 外部动作，限制在 `kio_host_path_service.*` 一个文件里）
- 端口映射改为**芯片 + 连线拓扑**：左列容器端口、右列宿主绑定，未发布的端口单独成组；
  连线只是装饰，信息全部由文字承载
- 破坏性操作统一走 `ConfirmDialog`（固定句式 + 必填的后果说明）；
  所有操作结果走唯一的 `OperationMessage` 通知通道

**权限**：见上方 ⚠️ 说明。次版本号从 0.3.0 提升到 0.4.0。

---

## 三期

UI 收口，**保持只读**（[ARCH_V3.md](ARCH_V3.md)）。

- 组件归一：状态徽标（`StatusChip`）、可复制字段（`CopyableText`）、空状态（`EmptyPlaceholder`）
  收敛为唯一实现；状态语义色集中到 `StatusPalette`
- 容器详情拆分为 概览 / 资源 / 网络 / 挂载 / 日志（占位）分区
- 镜像详情：层列表默认折叠前 5 层、多 tag 以 chip 呈现
- 数据可视化使用独立色板（亮/暗两套，过 WCAG AA），Storage 增加堆叠条
- 排版与响应：详情页限宽、数值右对齐、统计卡按窗口宽度重排
- 新增 i18n 裸字符串 lint 与 `CHANGELOG.md`

**权限**：无变化。三期不新增任何非 `GET` 请求，不引入提权机制（决策记录见 ARCH_V3.md §1.3）。

---

## 二期

可交互的只读 Dashboard（[ARCH_V2.md](ARCH_V2.md)）。

- 卡片导航：容器 / 镜像列表 → 详情页（页内 `StackView`，不依赖宿主 KCM 的 push/pop）
- 搜索 / 状态过滤 / 排序（条件保存在 proxy，后台刷新不重置）
- 统一状态体系：State（机器可读）/ Status（Docker 摘要）/ Health（独立语义）严格分离，
  颜色只作辅助，始终配文本与图标
- 刷新策略集中到 `refresh_policy.h`：5s 高频 + 30s storage + 手动刷新，
  Last Updated / Update failed / Data is stale，后台刷新不清空列表
- 容器资源监控：CPU / 内存 / 网络 / 块 IO，5s 采样、内存中保留 60 点，离开详情页立即停止
- Docker 磁盘占用（`GET /system/df`）
- 国际化：翻译域 `kcm_docker`，随附完整简体中文翻译
- 只读边界强化：Environment / Labels 默认折叠；剪贴板只提供标识类字段

**权限**：无变化，仍为只读。

---

## 一期

只读状态面板（[ARCH_V1.md](ARCH_V1.md)）。

- Docker Engine 只读访问：`/_ping`、`/version`、`/info`、`/containers/json`、`/images/json`
- 异步 `QLocalSocket` HTTP 客户端（无阻塞 I/O）、API 版本协商、请求去重
- Engine 概要、容器列表、镜像列表
- 分层：DTO → Domain → Model → UI，Backend 不依赖 UI，UI 不解析 Docker JSON

**权限**：只读；要求当前用户对 Docker socket 有读权限。
注意：**若使用系统级（root daemon）Docker 并把用户加入 docker 组，该组等价于 root 权限。**
本项目的目标部署形态是 rootless Docker（socket 由用户自己拥有），此时不涉及该风险。
